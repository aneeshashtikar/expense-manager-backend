spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/expensemanger
    username: aneeshashtikar
    password: root@123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   # options: none, validate, update, create, create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
